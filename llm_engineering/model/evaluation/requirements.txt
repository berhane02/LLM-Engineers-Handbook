# Evaluation requirements - only packages NOT in SageMaker HuggingFace container
# Container comes with: transformers, torch, tokenizers, huggingface-hub, numpy, scipy, 
#                       pandas, tqdm, safetensors, accelerate, datasets

# Evaluation-specific packages
vllm==0.6.1.post2
openai>=1.55.3